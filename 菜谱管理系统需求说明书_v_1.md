# 菜谱管理系统需求说明书 v1.0

## 1. 项目名称

菜谱分享与管理系统

## 2. 项目背景

随着互联网生活服务类平台的发展，越来越多用户希望通过网站查看、收藏和分享菜谱。传统纸质菜谱或零散的社交平台内容不方便统一管理，也不方便按照菜名、食材、分类进行检索。

本项目拟开发一个基于 Web 的菜谱管理系统，用户可以在系统中注册登录、发布菜谱、浏览菜谱、搜索菜谱、收藏菜谱和评论菜谱。管理员可以对用户、菜谱和分类进行基础管理。

本项目同时作为前后端综合实践项目，用于回顾和训练 Vue、Spring Boot、MySQL、前后端接口联调、数据库设计、规范化开发流程等能力。

## 3. 项目目标

### 3.1 学习目标

1. 回顾前端 Vue 项目的基本开发流程。
2. 回顾后端 Spring Boot 项目的基本开发流程。
3. 掌握 MySQL 数据库表设计、主外键关系和基础 SQL 操作。
4. 掌握前后端分离项目中的接口设计和接口联调方式。
5. 学习相对规范的软件开发流程，包括需求文档、数据库设计、接口文档、编码实现、测试和项目总结。

### 3.2 系统目标

1. 实现一个完整可运行的菜谱分享网站。
2. 普通用户可以发布、浏览、搜索、收藏和评论菜谱。
3. 管理员可以对系统中的用户、菜谱和分类进行管理。
4. 页面风格尽量美观、灵动，具有较好的视觉展示效果。
5. 后端业务逻辑清晰，接口结构规范，数据库设计合理。

## 4. 技术栈

### 4.1 前端技术栈

- Vue 3
- Vite
- Vue Router
- Pinia
- Axios
- Element Plus
- HTML / CSS / JavaScript

### 4.2 后端技术栈

- Spring Boot
- Spring Web
- MyBatis-Plus
- MySQL
- Lombok
- JWT 登录认证
- Swagger / Knife4j 接口文档

### 4.3 开发工具

- IntelliJ IDEA
- Visual Studio Code
- Navicat / DataGrip / MySQL Workbench
- Postman / Apifox
- Git

## 5. 用户角色

系统主要包含两类用户角色。

### 5.1 普通用户

普通用户可以注册登录系统，浏览菜谱，搜索菜谱，查看菜谱详情，发布自己的菜谱，修改或删除自己发布的菜谱，收藏喜欢的菜谱，并对菜谱发表评论。

### 5.2 管理员

管理员可以登录后台管理页面，对用户、菜谱、分类和评论进行管理。管理员可以删除违规菜谱、禁用异常用户、维护菜谱分类。

## 6. 功能模块

## 6.1 用户模块

### 6.1.1 用户注册

用户可以通过用户名、密码、手机号或邮箱完成注册。

基础字段包括：

- 用户名
- 密码
- 昵称
- 手机号或邮箱

注册时需要进行以下校验：

1. 用户名不能为空。
2. 密码不能为空。
3. 用户名不能重复。
4. 密码长度需要满足基础要求，例如不少于 6 位。

### 6.1.2 用户登录

用户可以使用用户名和密码登录系统。

登录成功后，后端返回用户基本信息和 token。前端保存 token，用于后续访问需要登录权限的接口。

### 6.1.3 用户退出登录

用户点击退出登录后，前端清除本地保存的 token 和用户信息，并跳转到登录页或首页。

### 6.1.4 个人信息查看

用户可以查看自己的基础信息，例如用户名、昵称、头像、注册时间等。

### 6.1.5 个人信息修改

用户可以修改昵称、头像、简介等信息。

## 6.2 菜谱模块

### 6.2.1 发布菜谱

登录用户可以发布新的菜谱。

发布菜谱需要填写以下信息：

- 菜谱名称
- 菜谱分类
- 菜谱封面图
- 简介
- 所需食材
- 制作步骤
- 烹饪时间
- 难度等级
- 小贴士

发布后，菜谱进入系统展示列表。

第一版可以先不做复杂审核，默认发布成功后直接展示。后续如果时间充足，可以增加管理员审核功能。

### 6.2.2 菜谱列表

用户可以在首页或菜谱列表页查看菜谱。

列表中展示：

- 菜谱封面图
- 菜谱名称
- 作者昵称
- 分类
- 浏览量
- 收藏数
- 发布时间

### 6.2.3 菜谱详情

用户点击某个菜谱后，可以进入详情页查看完整内容。

详情页展示：

- 菜谱封面图
- 菜谱名称
- 作者信息
- 分类
- 食材清单
- 制作步骤
- 小贴士
- 浏览量
- 收藏按钮
- 评论区

### 6.2.4 修改菜谱

用户只能修改自己发布的菜谱。

管理员可以修改或下架所有菜谱。

### 6.2.5 删除菜谱

用户可以删除自己发布的菜谱。

管理员可以删除任何违规菜谱。

删除方式建议采用逻辑删除，即数据库中保留数据，通过 `deleted` 字段标记是否删除。

### 6.2.6 菜谱搜索

用户可以根据关键词搜索菜谱。

搜索范围包括：

- 菜谱名称
- 菜谱简介
- 食材名称

### 6.2.7 菜谱分类筛选

用户可以按照分类筛选菜谱，例如：

- 家常菜
- 早餐
- 午餐
- 晚餐
- 甜品
- 汤类
- 川菜
- 粤菜
- 减脂餐

## 6.3 分类模块

### 6.3.1 分类列表

首页和发布菜谱页需要展示分类列表。

### 6.3.2 分类管理

管理员可以新增、修改、删除分类。

分类字段包括：

- 分类名称
- 分类图标
- 分类排序
- 分类状态

## 6.4 收藏模块

### 6.4.1 收藏菜谱

登录用户可以收藏自己喜欢的菜谱。

同一个用户不能重复收藏同一个菜谱。

### 6.4.2 取消收藏

用户可以取消已经收藏的菜谱。

### 6.4.3 我的收藏

用户可以在个人中心查看自己收藏过的菜谱列表。

## 6.5 评论模块

### 6.5.1 发表评论

登录用户可以在菜谱详情页发表评论。

评论内容不能为空。

### 6.5.2 查看评论

菜谱详情页展示该菜谱下的评论列表。

### 6.5.3 删除评论

用户可以删除自己的评论。

管理员可以删除任何不合适的评论。

## 6.6 后台管理模块

后台管理模块面向管理员。

### 6.6.1 用户管理

管理员可以查看用户列表，禁用或启用用户。

用户列表字段包括：

- 用户 ID
- 用户名
- 昵称
- 角色
- 状态
- 注册时间

### 6.6.2 菜谱管理

管理员可以查看所有菜谱，删除违规菜谱，修改菜谱状态。

### 6.6.3 分类管理

管理员可以维护菜谱分类。

### 6.6.4 评论管理

管理员可以查看和删除评论。

## 7. 页面设计

## 7.1 前台页面

### 7.1.1 首页

首页是系统的主要展示页面，需要具有较好的视觉效果。

首页内容包括：

1. 顶部导航栏。
2. 大搜索区域。
3. 分类快捷入口。
4. 推荐菜谱区域。
5. 最新菜谱区域。
6. 热门菜谱区域。

设计建议：

- 使用大图横幅。
- 使用卡片式菜谱布局。
- 图片圆角展示。
- 鼠标悬浮时卡片轻微上浮。
- 颜色风格温暖、清爽，适合美食主题。

### 7.1.2 登录页

用户输入用户名和密码完成登录。

### 7.1.3 注册页

用户填写用户名、密码、确认密码、昵称等信息完成注册。

### 7.1.4 菜谱列表页

展示所有菜谱，支持关键词搜索和分类筛选。

### 7.1.5 菜谱详情页

展示菜谱完整内容，包括食材、步骤、评论和收藏功能。

### 7.1.6 发布菜谱页

登录用户填写菜谱信息并提交。

### 7.1.7 我的菜谱页

展示当前用户发布过的菜谱，支持编辑和删除。

### 7.1.8 我的收藏页

展示当前用户收藏过的菜谱。

### 7.1.9 个人中心页

展示和修改个人信息。

## 7.2 后台页面

### 7.2.1 后台首页

展示系统基础统计信息，例如：

- 用户数量
- 菜谱数量
- 评论数量
- 分类数量

### 7.2.2 用户管理页

管理员查看和管理用户。

### 7.2.3 菜谱管理页

管理员查看和管理全部菜谱。

### 7.2.4 分类管理页

管理员维护分类。

### 7.2.5 评论管理页

管理员查看和删除评论。

## 8. 数据库初步设计

第一版建议设计以下数据表：

1. user 用户表
2. category 分类表
3. recipe 菜谱表
4. recipe_ingredient 菜谱食材表
5. recipe_step 菜谱步骤表
6. favorite 收藏表
7. comment 评论表

## 8.1 user 用户表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| username | varchar | 用户名 |
| password | varchar | 密码，加密存储 |
| nickname | varchar | 昵称 |
| avatar | varchar | 头像地址 |
| email | varchar | 邮箱 |
| phone | varchar | 手机号 |
| role | varchar | 角色：USER / ADMIN |
| status | tinyint | 状态：1 正常，0 禁用 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

## 8.2 category 分类表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| name | varchar | 分类名称 |
| icon | varchar | 分类图标 |
| sort | int | 排序值 |
| status | tinyint | 状态：1 启用，0 禁用 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

## 8.3 recipe 菜谱表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 作者用户 ID |
| category_id | bigint | 分类 ID |
| title | varchar | 菜谱名称 |
| cover_image | varchar | 封面图 |
| description | varchar | 菜谱简介 |
| difficulty | varchar | 难度：简单 / 中等 / 困难 |
| cooking_time | int | 烹饪时间，单位分钟 |
| tips | text | 小贴士 |
| view_count | int | 浏览量 |
| favorite_count | int | 收藏数 |
| comment_count | int | 评论数 |
| status | tinyint | 状态：1 正常，0 下架 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

## 8.4 recipe_ingredient 菜谱食材表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| recipe_id | bigint | 菜谱 ID |
| name | varchar | 食材名称 |
| amount | varchar | 用量，例如 200g、1勺 |
| sort | int | 排序 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

## 8.5 recipe_step 菜谱步骤表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| recipe_id | bigint | 菜谱 ID |
| step_no | int | 步骤序号 |
| content | text | 步骤说明 |
| image | varchar | 步骤图片 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

## 8.6 favorite 收藏表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 用户 ID |
| recipe_id | bigint | 菜谱 ID |
| create_time | datetime | 收藏时间 |
| deleted | tinyint | 是否删除 |

## 8.7 comment 评论表

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 ID |
| user_id | bigint | 用户 ID |
| recipe_id | bigint | 菜谱 ID |
| content | text | 评论内容 |
| status | tinyint | 状态：1 正常，0 隐藏 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| deleted | tinyint | 是否删除 |

## 9. 接口初步设计

## 9.1 用户接口

| 接口名称 | 请求方式 | 接口路径 | 说明 |
|---|---|---|---|
| 用户注册 | POST | /api/user/register | 注册新用户 |
| 用户登录 | POST | /api/user/login | 用户登录 |
| 获取当前用户信息 | GET | /api/user/current | 根据 token 获取当前用户 |
| 修改个人信息 | PUT | /api/user/profile | 修改当前用户信息 |
| 修改密码 | PUT | /api/user/password | 修改当前用户密码 |

## 9.2 菜谱接口

| 接口名称 | 请求方式 | 接口路径 | 说明 |
|---|---|---|---|
| 发布菜谱 | POST | /api/recipe | 新增菜谱 |
| 查询菜谱列表 | GET | /api/recipe/list | 分页查询菜谱 |
| 查询菜谱详情 | GET | /api/recipe/{id} | 查询单个菜谱详情 |
| 修改菜谱 | PUT | /api/recipe/{id} | 修改菜谱 |
| 删除菜谱 | DELETE | /api/recipe/{id} | 删除菜谱 |
| 查询我的菜谱 | GET | /api/recipe/my | 查询当前用户发布的菜谱 |

## 9.3 分类接口

| 接口名称 | 请求方式 | 接口路径 | 说明 |
|---|---|---|---|
| 查询分类列表 | GET | /api/category/list | 查询所有启用分类 |
| 新增分类 | POST | /api/admin/category | 管理员新增分类 |
| 修改分类 | PUT | /api/admin/category/{id} | 管理员修改分类 |
| 删除分类 | DELETE | /api/admin/category/{id} | 管理员删除分类 |

## 9.4 收藏接口

| 接口名称 | 请求方式 | 接口路径 | 说明 |
|---|---|---|---|
| 收藏菜谱 | POST | /api/favorite/{recipeId} | 收藏指定菜谱 |
| 取消收藏 | DELETE | /api/favorite/{recipeId} | 取消收藏指定菜谱 |
| 我的收藏 | GET | /api/favorite/my | 查询我的收藏列表 |

## 9.5 评论接口

| 接口名称 | 请求方式 | 接口路径 | 说明 |
|---|---|---|---|
| 发表评论 | POST | /api/comment | 新增评论 |
| 查询菜谱评论 | GET | /api/comment/recipe/{recipeId} | 查询某菜谱评论 |
| 删除评论 | DELETE | /api/comment/{id} | 删除评论 |

## 9.6 管理员接口

| 接口名称 | 请求方式 | 接口路径 | 说明 |
|---|---|---|---|
| 用户列表 | GET | /api/admin/user/list | 管理员分页查询用户 |
| 禁用用户 | PUT | /api/admin/user/{id}/disable | 禁用用户 |
| 启用用户 | PUT | /api/admin/user/{id}/enable | 启用用户 |
| 菜谱管理列表 | GET | /api/admin/recipe/list | 管理员查询所有菜谱 |
| 下架菜谱 | PUT | /api/admin/recipe/{id}/disable | 下架菜谱 |
| 删除评论 | DELETE | /api/admin/comment/{id} | 管理员删除评论 |

## 10. 非功能性需求

## 10.1 易用性

1. 页面操作简单清晰。
2. 发布菜谱表单字段明确。
3. 搜索和分类筛选入口明显。
4. 用户未登录时访问需要登录的功能，应提示登录。

## 10.2 安全性

1. 密码不能明文存储，应进行加密处理。
2. 需要登录的接口必须校验 token。
3. 用户只能修改和删除自己的菜谱。
4. 管理员接口只能管理员访问。

## 10.3 可维护性

1. 前后端代码按模块划分。
2. 后端采用 Controller、Service、Mapper 分层结构。
3. 前端按 views、components、api、store、router 等目录组织。
4. 接口返回格式统一。

## 10.4 性能要求

1. 菜谱列表需要分页查询。
2. 首页推荐和热门菜谱查询数量不宜过多。
3. 数据库常用查询字段可以建立索引，例如 username、recipe title、category_id、user_id。

## 11. 统一返回格式设计

后端接口建议统一返回格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

常见状态码：

| code | 含义 |
|---|---|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录或 token 无效 |
| 403 | 没有权限 |
| 404 | 数据不存在 |
| 500 | 服务器错误 |

## 12. 项目目录初步规划

## 12.1 后端目录

```text
recipe-system-backend
├── src/main/java/com/example/recipe
│   ├── controller
│   ├── service
│   ├── service/impl
│   ├── mapper
│   ├── entity
│   ├── dto
│   ├── vo
│   ├── config
│   ├── common
│   ├── exception
│   └── RecipeApplication.java
└── src/main/resources
    ├── application.yml
    └── mapper
```

## 12.2 前端目录

```text
recipe-system-frontend
├── src
│   ├── api
│   ├── assets
│   ├── components
│   ├── router
│   ├── store
│   ├── views
│   ├── utils
│   ├── App.vue
│   └── main.js
├── package.json
└── vite.config.js
```

## 13. 开发阶段规划

## 第一阶段：需求与设计

目标：明确系统功能范围，完成需求文档、数据库设计和接口设计。

产出：

1. 需求说明书。
2. 数据库表结构 SQL。
3. 接口文档。
4. 页面原型草图或页面规划。

## 第二阶段：后端基础框架搭建

目标：搭建 Spring Boot 项目，连接 MySQL，完成基础代码结构。

产出：

1. Spring Boot 项目。
2. MySQL 数据库连接。
3. 统一返回结果类。
4. 全局异常处理。
5. 基础实体类和 Mapper。

## 第三阶段：用户与登录模块

目标：完成注册、登录、token 校验和用户信息接口。

产出：

1. 用户注册接口。
2. 用户登录接口。
3. 当前用户信息接口。
4. 登录拦截器或过滤器。

## 第四阶段：菜谱核心模块

目标：完成菜谱发布、查询、详情、修改、删除。

产出：

1. 发布菜谱接口。
2. 菜谱列表接口。
3. 菜谱详情接口。
4. 修改菜谱接口。
5. 删除菜谱接口。

## 第五阶段：分类、收藏、评论模块

目标：完善系统功能。

产出：

1. 分类接口。
2. 收藏接口。
3. 评论接口。

## 第六阶段：前端页面开发

目标：完成 Vue 前端页面和接口联调。

产出：

1. 首页。
2. 登录注册页。
3. 菜谱列表页。
4. 菜谱详情页。
5. 发布菜谱页。
6. 个人中心页。
7. 后台管理页。

## 第七阶段：测试与优化

目标：修复 bug，优化页面和业务逻辑。

产出：

1. 功能测试记录。
2. bug 修复记录。
3. 页面优化。
4. 项目说明文档。

## 14. 第一版必须完成的功能范围

为了保证项目能按时完成，第一版必须完成以下功能：

1. 用户注册。
2. 用户登录。
3. 菜谱发布。
4. 菜谱列表查询。
5. 菜谱详情查询。
6. 菜谱修改。
7. 菜谱删除。
8. 菜谱分类查询。
9. 菜谱关键词搜索。
10. 我的菜谱。
11. 收藏菜谱。
12. 评论菜谱。
13. 管理员查看用户列表。
14. 管理员管理菜谱。
15. 管理员管理分类。

## 15. 可以后续扩展的功能

如果时间充足，可以增加以下功能：

1. 菜谱审核。
2. 点赞功能。
3. 浏览历史。
4. 个性化推荐。
5. 标签系统。
6. 营养信息。
7. 移动端适配。
8. 图片上传到对象存储。
9. AI 菜谱推荐。

## 16. 验收标准

项目完成时，需要满足以下标准：

1. 前端页面可以正常访问。
2. 用户可以完成注册和登录。
3. 登录用户可以发布菜谱。
4. 用户可以查看菜谱列表和详情。
5. 用户可以通过关键词搜索菜谱。
6. 用户可以按照分类筛选菜谱。
7. 用户可以收藏和评论菜谱。
8. 用户只能修改和删除自己的菜谱。
9. 管理员可以进入后台管理页面。
10. 管理员可以管理用户、菜谱和分类。
11. 数据库表结构清晰，数据关系正确。
12. 前后端接口能够正常联调。
13. 项目代码结构清楚，具备基本注释。
14. 项目可以在本地完整运行。

## 17. 当前阶段结论

本项目适合作为 Vue + Spring Boot + MySQL 的综合练习项目。系统业务逻辑完整，但难度可控。第一阶段应先完成需求文档、数据库设计和接口设计，再开始搭建代码工程。

当前下一步任务：

完成数据库设计，输出 MySQL 建表 SQL，并确定后端实体类与前端页面之间的数据对应关系。

