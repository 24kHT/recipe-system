# 菜谱管理系统数据库设计 v1.0

## 1. 数据库设计目标

本数据库用于支撑“菜谱分享与管理系统”的核心业务。

系统需要支持以下功能：

1. 用户注册、登录、个人信息管理。
2. 用户发布菜谱。
3. 用户浏览、搜索、筛选菜谱。
4. 菜谱包含基本信息、食材清单和制作步骤。
5. 用户可以收藏菜谱。
6. 用户可以评论菜谱。
7. 管理员可以管理用户、菜谱、分类和评论。

因此，数据库设计需要满足：

1. 数据结构清晰。
2. 表之间关系明确。
3. 支持后续接口开发。
4. 支持分页查询、关键词搜索、分类筛选。
5. 支持逻辑删除，避免误删真实数据。
6. 适合作为课程作业项目，不做过度复杂设计。

---

## 2. 核心数据对象分析

在菜谱系统中，核心数据对象包括：

| 数据对象 | 说明 |
|---|---|
| 用户 | 系统注册用户，包括普通用户和管理员 |
| 分类 | 菜谱分类，例如家常菜、早餐、甜品 |
| 菜谱 | 用户发布的菜谱主体信息 |
| 食材 | 某个菜谱需要的食材和用量 |
| 步骤 | 某个菜谱的制作步骤 |
| 收藏 | 用户收藏菜谱的记录 |
| 评论 | 用户对菜谱发表的评论 |

所以第一版数据库设计为 7 张核心表：

1. `user`：用户表
2. `category`：分类表
3. `recipe`：菜谱表
4. `recipe_ingredient`：菜谱食材表
5. `recipe_step`：菜谱步骤表
6. `favorite`：收藏表
7. `comment`：评论表

---

## 3. 表关系说明

## 3.1 用户与菜谱

一个用户可以发布多个菜谱。

一个菜谱只属于一个用户。

关系：

```text
user 1 ---- n recipe
```

实现方式：

在 `recipe` 表中添加 `user_id` 字段，表示该菜谱的作者。

---

## 3.2 分类与菜谱

一个分类下可以有多个菜谱。

一个菜谱只属于一个分类。

关系：

```text
category 1 ---- n recipe
```

实现方式：

在 `recipe` 表中添加 `category_id` 字段，表示该菜谱所属分类。

---

## 3.3 菜谱与食材

一个菜谱可以有多个食材。

一个食材记录只属于一个菜谱。

关系：

```text
recipe 1 ---- n recipe_ingredient
```

为什么食材不直接放到 `recipe` 表里？

因为一个菜谱有多个食材，例如：

```text
鸡蛋 2个
番茄 2个
盐 适量
糖 少许
```

如果都塞进 `recipe` 表的一个字段里，后期不方便单独增删改查，也不规范。

---

## 3.4 菜谱与步骤

一个菜谱可以有多个制作步骤。

一个步骤只属于一个菜谱。

关系：

```text
recipe 1 ---- n recipe_step
```

为什么步骤也要单独建表？

因为每个步骤都有自己的序号、文字说明和步骤图片。如果直接写在一个大文本字段里，前端展示和后端维护都不方便。

---

## 3.5 用户与收藏

一个用户可以收藏多个菜谱。

一个菜谱也可以被多个用户收藏。

这在业务上是多对多关系：

```text
user n ---- n recipe
```

数据库中一般不用直接做多对多，而是增加一张中间表 `favorite`。

实际关系变成：

```text
user 1 ---- n favorite
recipe 1 ---- n favorite
```

`favorite` 表记录：哪个用户收藏了哪个菜谱。

---

## 3.6 用户与评论、菜谱与评论

一个用户可以发表多条评论。

一个菜谱可以有多条评论。

关系：

```text
user 1 ---- n comment
recipe 1 ---- n comment
```

`comment` 表记录：哪个用户在什么菜谱下发表了什么评论。

---

## 4. 数据库表总览

| 表名 | 中文名 | 作用 |
|---|---|---|
| user | 用户表 | 保存用户账号、密码、角色、状态等信息 |
| category | 分类表 | 保存菜谱分类信息 |
| recipe | 菜谱表 | 保存菜谱主体信息 |
| recipe_ingredient | 菜谱食材表 | 保存菜谱所需食材 |
| recipe_step | 菜谱步骤表 | 保存菜谱制作步骤 |
| favorite | 收藏表 | 保存用户收藏菜谱的关系 |
| comment | 评论表 | 保存用户对菜谱的评论 |

---

## 5. 通用字段设计

大部分业务表建议包含以下通用字段：

| 字段名 | 说明 |
|---|---|
| id | 主键 ID |
| create_time | 创建时间 |
| update_time | 更新时间 |
| deleted | 是否逻辑删除 |

部分表还需要：

| 字段名 | 说明 |
|---|---|
| status | 状态，例如正常、禁用、下架、隐藏 |
| sort | 排序值 |

### 逻辑删除说明

逻辑删除指的是数据不从数据库中真正删除，而是通过字段标记为已删除。

例如：

```text
deleted = 0 表示未删除
deleted = 1 表示已删除
```

优点：

1. 防止误删数据。
2. 方便管理员后期追溯。
3. 适合课程项目展示规范性。

---

## 6. 表字段详细设计

## 6.1 user 用户表

### 表作用

保存系统用户信息，包括普通用户和管理员。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| username | varchar(50) | 是 | 用户名，唯一 |
| password | varchar(255) | 是 | 密码，加密存储 |
| nickname | varchar(50) | 否 | 昵称 |
| avatar | varchar(255) | 否 | 头像地址 |
| email | varchar(100) | 否 | 邮箱 |
| phone | varchar(20) | 否 | 手机号 |
| role | varchar(20) | 是 | 角色：USER / ADMIN |
| status | tinyint | 是 | 状态：1 正常，0 禁用 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 是 | 更新时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

1. `username` 需要唯一，防止重复注册。
2. `password` 不应该明文存储，后端需要加密。
3. `role` 用来区分普通用户和管理员。
4. `status` 用来实现管理员禁用用户。

---

## 6.2 category 分类表

### 表作用

保存菜谱分类信息。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| name | varchar(50) | 是 | 分类名称 |
| icon | varchar(255) | 否 | 分类图标地址或图标名 |
| sort | int | 是 | 排序值，越小越靠前 |
| status | tinyint | 是 | 状态：1 启用，0 禁用 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 是 | 更新时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

1. 分类用于首页展示和菜谱筛选。
2. `sort` 用于控制分类显示顺序。
3. `status` 用于控制分类是否启用。

---

## 6.3 recipe 菜谱表

### 表作用

保存菜谱主体信息。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| user_id | bigint | 是 | 作者用户 ID |
| category_id | bigint | 是 | 分类 ID |
| title | varchar(100) | 是 | 菜谱名称 |
| cover_image | varchar(255) | 否 | 封面图地址 |
| description | varchar(500) | 否 | 菜谱简介 |
| difficulty | varchar(20) | 否 | 难度：简单 / 中等 / 困难 |
| cooking_time | int | 否 | 烹饪时间，单位分钟 |
| tips | text | 否 | 小贴士 |
| view_count | int | 是 | 浏览量 |
| favorite_count | int | 是 | 收藏数 |
| comment_count | int | 是 | 评论数 |
| status | tinyint | 是 | 状态：1 正常，0 下架 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 是 | 更新时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

1. `user_id` 关联发布菜谱的用户。
2. `category_id` 关联菜谱分类。
3. `view_count` 用于统计浏览量。
4. `favorite_count` 可以提升查询效率，不用每次都去收藏表统计。
5. `comment_count` 可以提升列表页显示效率。
6. `status` 用于管理员下架菜谱。

---

## 6.4 recipe_ingredient 菜谱食材表

### 表作用

保存菜谱所需食材。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| recipe_id | bigint | 是 | 菜谱 ID |
| name | varchar(100) | 是 | 食材名称 |
| amount | varchar(100) | 否 | 食材用量，例如 200g、1勺 |
| sort | int | 是 | 排序 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 是 | 更新时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

一个菜谱可以对应多条食材记录。

例如番茄炒蛋：

| recipe_id | name | amount |
|---|---|---|
| 1 | 番茄 | 2个 |
| 1 | 鸡蛋 | 3个 |
| 1 | 盐 | 适量 |

---

## 6.5 recipe_step 菜谱步骤表

### 表作用

保存菜谱制作步骤。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| recipe_id | bigint | 是 | 菜谱 ID |
| step_no | int | 是 | 步骤序号 |
| content | text | 是 | 步骤说明 |
| image | varchar(255) | 否 | 步骤图片地址 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 是 | 更新时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

一个菜谱可以对应多个步骤。

例如：

| recipe_id | step_no | content |
|---|---|---|
| 1 | 1 | 番茄洗净切块 |
| 1 | 2 | 鸡蛋打散 |
| 1 | 3 | 热锅倒油炒鸡蛋 |

---

## 6.6 favorite 收藏表

### 表作用

保存用户收藏菜谱的记录。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| user_id | bigint | 是 | 用户 ID |
| recipe_id | bigint | 是 | 菜谱 ID |
| create_time | datetime | 是 | 收藏时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

1. `user_id + recipe_id` 应该建立唯一约束，防止同一个用户重复收藏同一个菜谱。
2. 用户取消收藏时，可以逻辑删除该收藏记录。
3. 用户再次收藏时，可以恢复原记录，也可以新增记录。第一版建议简单处理：如果已存在未删除记录，就提示已经收藏。

---

## 6.7 comment 评论表

### 表作用

保存用户对菜谱的评论。

### 字段设计

| 字段名 | 类型 | 是否必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键 ID |
| user_id | bigint | 是 | 评论用户 ID |
| recipe_id | bigint | 是 | 菜谱 ID |
| content | text | 是 | 评论内容 |
| status | tinyint | 是 | 状态：1 正常，0 隐藏 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 是 | 更新时间 |
| deleted | tinyint | 是 | 是否删除：0 否，1 是 |

### 设计说明

1. 评论必须关联用户和菜谱。
2. 普通用户只能删除自己的评论。
3. 管理员可以隐藏或删除任何评论。

---

## 7. MySQL 建表 SQL

## 7.1 创建数据库

```sql
CREATE DATABASE IF NOT EXISTS recipe_system
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_general_ci;

USE recipe_system;
```

---

## 7.2 用户表

```sql
CREATE TABLE IF NOT EXISTS `user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `username` VARCHAR(50) NOT NULL COMMENT '用户名',
    `password` VARCHAR(255) NOT NULL COMMENT '密码，加密存储',
    `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
    `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像地址',
    `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
    `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
    `role` VARCHAR(20) NOT NULL DEFAULT 'USER' COMMENT '角色：USER普通用户，ADMIN管理员',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常，0禁用',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    UNIQUE KEY `uk_user_username` (`username`),
    KEY `idx_user_role` (`role`),
    KEY `idx_user_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

---

## 7.3 分类表

```sql
CREATE TABLE IF NOT EXISTS `category` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `name` VARCHAR(50) NOT NULL COMMENT '分类名称',
    `icon` VARCHAR(255) DEFAULT NULL COMMENT '分类图标',
    `sort` INT NOT NULL DEFAULT 0 COMMENT '排序值，越小越靠前',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用，0禁用',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    UNIQUE KEY `uk_category_name` (`name`),
    KEY `idx_category_status` (`status`),
    KEY `idx_category_sort` (`sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜谱分类表';
```

---

## 7.4 菜谱表

```sql
CREATE TABLE IF NOT EXISTS `recipe` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `user_id` BIGINT NOT NULL COMMENT '作者用户ID',
    `category_id` BIGINT NOT NULL COMMENT '分类ID',
    `title` VARCHAR(100) NOT NULL COMMENT '菜谱名称',
    `cover_image` VARCHAR(255) DEFAULT NULL COMMENT '封面图地址',
    `description` VARCHAR(500) DEFAULT NULL COMMENT '菜谱简介',
    `difficulty` VARCHAR(20) DEFAULT NULL COMMENT '难度：简单/中等/困难',
    `cooking_time` INT DEFAULT NULL COMMENT '烹饪时间，单位分钟',
    `tips` TEXT DEFAULT NULL COMMENT '小贴士',
    `view_count` INT NOT NULL DEFAULT 0 COMMENT '浏览量',
    `favorite_count` INT NOT NULL DEFAULT 0 COMMENT '收藏数',
    `comment_count` INT NOT NULL DEFAULT 0 COMMENT '评论数',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常，0下架',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    KEY `idx_recipe_user_id` (`user_id`),
    KEY `idx_recipe_category_id` (`category_id`),
    KEY `idx_recipe_title` (`title`),
    KEY `idx_recipe_status` (`status`),
    KEY `idx_recipe_create_time` (`create_time`),
    CONSTRAINT `fk_recipe_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`),
    CONSTRAINT `fk_recipe_category` FOREIGN KEY (`category_id`) REFERENCES `category` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜谱表';
```

---

## 7.5 菜谱食材表

```sql
CREATE TABLE IF NOT EXISTS `recipe_ingredient` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `recipe_id` BIGINT NOT NULL COMMENT '菜谱ID',
    `name` VARCHAR(100) NOT NULL COMMENT '食材名称',
    `amount` VARCHAR(100) DEFAULT NULL COMMENT '用量，例如200g、1勺',
    `sort` INT NOT NULL DEFAULT 0 COMMENT '排序',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    KEY `idx_ingredient_recipe_id` (`recipe_id`),
    KEY `idx_ingredient_name` (`name`),
    CONSTRAINT `fk_ingredient_recipe` FOREIGN KEY (`recipe_id`) REFERENCES `recipe` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜谱食材表';
```

---

## 7.6 菜谱步骤表

```sql
CREATE TABLE IF NOT EXISTS `recipe_step` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `recipe_id` BIGINT NOT NULL COMMENT '菜谱ID',
    `step_no` INT NOT NULL COMMENT '步骤序号',
    `content` TEXT NOT NULL COMMENT '步骤说明',
    `image` VARCHAR(255) DEFAULT NULL COMMENT '步骤图片地址',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    KEY `idx_step_recipe_id` (`recipe_id`),
    KEY `idx_step_no` (`step_no`),
    CONSTRAINT `fk_step_recipe` FOREIGN KEY (`recipe_id`) REFERENCES `recipe` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜谱步骤表';
```

---

## 7.7 收藏表

```sql
CREATE TABLE IF NOT EXISTS `favorite` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `recipe_id` BIGINT NOT NULL COMMENT '菜谱ID',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    UNIQUE KEY `uk_favorite_user_recipe` (`user_id`, `recipe_id`),
    KEY `idx_favorite_user_id` (`user_id`),
    KEY `idx_favorite_recipe_id` (`recipe_id`),
    CONSTRAINT `fk_favorite_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`),
    CONSTRAINT `fk_favorite_recipe` FOREIGN KEY (`recipe_id`) REFERENCES `recipe` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收藏表';
```

---

## 7.8 评论表

```sql
CREATE TABLE IF NOT EXISTS `comment` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    `user_id` BIGINT NOT NULL COMMENT '评论用户ID',
    `recipe_id` BIGINT NOT NULL COMMENT '菜谱ID',
    `content` TEXT NOT NULL COMMENT '评论内容',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常，0隐藏',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：0否，1是',
    KEY `idx_comment_user_id` (`user_id`),
    KEY `idx_comment_recipe_id` (`recipe_id`),
    KEY `idx_comment_status` (`status`),
    KEY `idx_comment_create_time` (`create_time`),
    CONSTRAINT `fk_comment_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`),
    CONSTRAINT `fk_comment_recipe` FOREIGN KEY (`recipe_id`) REFERENCES `recipe` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';
```

---

## 8. 初始化测试数据

为了方便开发和测试，可以先插入一些基础分类和管理员账号。

### 8.1 初始化分类

```sql
INSERT INTO `category` (`name`, `icon`, `sort`, `status`) VALUES
('家常菜', 'home-food', 1, 1),
('早餐', 'breakfast', 2, 1),
('午餐', 'lunch', 3, 1),
('晚餐', 'dinner', 4, 1),
('甜品', 'dessert', 5, 1),
('汤类', 'soup', 6, 1),
('川菜', 'sichuan', 7, 1),
('粤菜', 'cantonese', 8, 1),
('减脂餐', 'fitness', 9, 1);
```

### 8.2 初始化管理员账号

注意：下面密码只是示例。真实开发时，密码应该由后端加密后再保存。

```sql
INSERT INTO `user` (`username`, `password`, `nickname`, `role`, `status`) VALUES
('admin', '123456', '系统管理员', 'ADMIN', 1);
```

---

## 9. 重要设计点解释

## 9.1 为什么菜谱表不直接存所有内容？

因为一个菜谱包含多个食材和多个步骤。

如果把食材和步骤全部塞进 `recipe` 表的一个字段中，虽然短期简单，但会带来问题：

1. 前端不好逐条展示。
2. 后端不好单独修改某个步骤。
3. 数据结构不清楚。
4. 不符合关系型数据库设计习惯。

所以拆成：

```text
recipe：保存菜谱基本信息
recipe_ingredient：保存食材列表
recipe_step：保存步骤列表
```

---

## 9.2 为什么收藏需要单独建表？

收藏表示“用户”和“菜谱”之间的关系。

一个用户可以收藏多个菜谱。

一个菜谱也可以被多个用户收藏。

这是典型多对多关系，所以需要中间表 `favorite`。

---

## 9.3 为什么评论需要同时保存 user_id 和 recipe_id？

因为一条评论必须知道：

1. 谁评论的。
2. 评论的是哪一道菜。
3. 评论内容是什么。
4. 评论时间是什么。

所以 `comment` 表必须包含 `user_id` 和 `recipe_id`。

---

## 9.4 是否一定要使用外键？

课程项目中可以使用外键，这样表关系更清楚。

但实际企业项目中，有些团队为了性能和灵活性，不一定在数据库层面建立外键，而是在业务代码里控制数据关系。

本项目为了学习数据库关系，建议第一版保留外键。

---

## 9.5 为什么要有状态字段？

例如：

1. 用户可能被禁用。
2. 分类可能被停用。
3. 菜谱可能被管理员下架。
4. 评论可能被管理员隐藏。

如果没有状态字段，只能删除数据，不方便管理。

---

## 10. 后端实体类对应关系

后端 Spring Boot 中，后续可以创建这些实体类：

| 数据表 | Java 实体类 |
|---|---|
| user | User |
| category | Category |
| recipe | Recipe |
| recipe_ingredient | RecipeIngredient |
| recipe_step | RecipeStep |
| favorite | Favorite |
| comment | Comment |

后端代码结构大致为：

```text
entity
├── User.java
├── Category.java
├── Recipe.java
├── RecipeIngredient.java
├── RecipeStep.java
├── Favorite.java
└── Comment.java
```

---

## 11. 前端页面与数据库关系

| 前端页面 | 主要使用的数据表 |
|---|---|
| 登录页 | user |
| 注册页 | user |
| 首页 | recipe、category、user |
| 菜谱列表页 | recipe、category、user |
| 菜谱详情页 | recipe、user、category、recipe_ingredient、recipe_step、comment、favorite |
| 发布菜谱页 | recipe、recipe_ingredient、recipe_step、category |
| 我的菜谱页 | recipe |
| 我的收藏页 | favorite、recipe |
| 个人中心页 | user |
| 后台用户管理页 | user |
| 后台菜谱管理页 | recipe、user、category |
| 后台分类管理页 | category |
| 后台评论管理页 | comment、user、recipe |

---

## 12. 当前阶段结论

本数据库设计可以支撑菜谱管理系统第一版的核心功能